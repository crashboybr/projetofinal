{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Schedule list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Teacherid</th>
                <th>Studentid</th>
                <th>Status</th>
                <th>Classat</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('schedule_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.teacherId }}</td>
                <td>{{ entity.studentId }}</td>
                <td>{{ entity.status }}</td>
                <td>{% if entity.classAt %}{{ entity.classAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('schedule_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('schedule_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('schedule_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
